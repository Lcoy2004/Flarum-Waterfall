// Eagerly import shared modules so they are registered in the module
// registry (chunked bundles resolve them via flarum.reg).
import './models/WaterfallImage';
import './models/WaterfallSet';
import './models/WaterfallUploadLog';
