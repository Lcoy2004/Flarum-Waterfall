// Eagerly import admin modules so they are registered in the module
// registry (chunked bundles resolve them via flarum.reg).
import '../common/common';

import './components/WaterfallUploadLogPanel';
import './components/WaterfallConfigTest';
